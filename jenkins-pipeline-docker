pipeline {
    agent any
    tools{
        maven 'local--mvn'
    }
    stages{
        stage ('build'){
            steps{
                sh 'mvn clean package'
                sh "/usr/bin/docker -H tcp://localhost:2375 build . -t tomcatwebapp:${env.BUILD_ID}"
            }
        }
    }
}